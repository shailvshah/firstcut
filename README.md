# firstcut
A project scaffolder that sets up backend, frontend, monorepo, tooling, infrastructure, and docs repos with first-principles engineering built in — DDD, TDD, interface-first design, pre-commit hooks, and CI — so any developer starts right, not just fast.
